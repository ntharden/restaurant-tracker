<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

  <main>
    <h2><%= title %></h2>
    <a href="/restaurants/new">Add a restaurant</a> <br>
    <ul>
      <% restaurants.forEach(restaurant => { %>
      <li>
         <%= restaurant.name %> <%= restaurant.thumbsUp ? '👍' : '👎' %>
         <a href="/restaurants/<%= restaurant._id %>">About</a> <%= restaurant.name %>
        <% }) %>
      </li>
    </ul>
    </main>

<%- include('../partials/footer') %>